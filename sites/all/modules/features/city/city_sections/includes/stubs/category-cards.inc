<?php
if (!empty($vars['state'])) {
  $vars['state'] = ', ' . $vars['state'];
}
$schoolsadblock = block_load('jci_dfp', 'jci-dfp-block-18');
$schoolsad = _block_get_renderable_array(_block_render_blocks(array($schoolsadblock)));
$healthadblock = block_load('jci_dfp', 'jci-dfp-block-17');
$healthad = _block_get_renderable_array(_block_render_blocks(array($healthadblock)));
$businessadblock = block_load('jci_dfp', 'jci-dfp-block-16');
$businessad = _block_get_renderable_array(_block_render_blocks(array($businessadblock)));
$realestateadblock = block_load('jci_dfp', 'jci-dfp-block-19');
$realestatead = _block_get_renderable_array(_block_render_blocks(array($realestateadblock)));

print '<div class="city-topics padded clearfix">';
	print '<ul>';
		print '<li class="half">';
			print '<span class="topic-icon">';
				print '<span class="circle-icon"><span class="sticker-orange-vertical"><i class="icon-education white"></i></span></span>';
            print '</span>';
			print '<span class="topic-content">';
				print '<h2><a href="' . $vars['url'] . '/schools">Schools</a></h2> ';
				print '<span class="in-city-state">';
					print 'In ' . $vars['city'] . $vars['state'];
				print '</span>';
				print '<span class="presented-by">';
					print drupal_render($schoolsad);
				print '</span>';
				print '<p>';
					print ' The quality of a school district is often a top factor for parents considering where to live. Strong school systems attract new residents and help their cities prosper. See how ' . $vars['city'] . ' rates.';
				print '</p>';
				print '<span class="topic-links">';
					print '<a href="' . $vars['url'] . '/schools/public-schools">Public Schools in ' . $vars['city'] . $vars['state'] . '</a><br/>';
					print '<a href="' . $vars['url'] . '/schools/private-schools">Private Schools in ' . $vars['city'] . $vars['state'] . '</a>';
				print '</span>';
			print '</span>';
		print '</li>';
		print '<li class="half">';
			print '<span class="topic-icon">';
				print '<span class="circle-icon"><span class="sticker-orange-vertical"><i class="icon-business white"></i></span></span>';
            print '</span>';
            print '<span class="topic-content">';
				print '<h2><a href="' . $vars['url'] . '/business">Business</a></h2> ';
				print '<span class="in-city-state">';
					print 'In ' . $vars['city'] . $vars['state'];
				print '</span>';
				print '<span class="presented-by">';
					print drupal_render($businessad);
				print '</span>';
				print '<p>';
					print 'Being a great place to live and being a great place to work often go hand-in-hand. Here’s what the business and employment climate is like in ' . $vars['city'] . '.';
				print '</p>';
				print '<span class="topic-links">';
					print '<a href="' . $vars['url'] . '/business/major-industries">Major Industries in ' . $vars['city'] . $vars['state'] . '</a><br/>';
					print '<a href="' . $vars['url'] . '/business/employment">Employment in ' . $vars['city'] . $vars['state'] . '</a>';
				print '</span>';
			print '</span>';
		print '</li>';
		print '<li class="half">';
			print '<span class="topic-icon">';
				print '<span class="circle-icon"><span class="sticker-orange-vertical"><i class="icon-health white"></i></span></span>';
            print '</span>';
            print '<span class="topic-content">';
				print '<h2><a href="' . $vars['url'] . '/health">Health</a></h2> ';
				print '<span class="in-city-state">';
					print 'In ' . $vars['city'] . $vars['state'];
				print '</span>';
				print '<span class="presented-by">';
					print drupal_render($healthad);
				print '</span>';
				print '<p>';
					print 'Access to quality hospitals, doctors and health-care providers is just part of what makes a healthy community. Here’s how to get and stay healthy in ' . $vars['city'] . '.';
				print '</p>';
				print '<span class="topic-links">';
					print '<a href="' . $vars['url'] . '/health/healthy-lifestyle">Healthy Lifestyles in ' . $vars['city'] . $vars['state'] . '</a><br/>';
					print '<a href="' . $vars['url'] . '/health/doctors">Doctors in ' . $vars['city'] . $vars['state'] . '</a><br/>';
					print '<a href="' . $vars['url'] . '/health/hospitals">Hospitals in ' . $vars['city'] . $vars['state'] . '</a>';
				print '</span>';
			print '</span>';
		print '</li>';
		print '<li class="half">';
			print '<span class="topic-icon">';
				print '<span class="circle-icon"><span class="sticker-orange-vertical"><i class="icon-real-estate white"></i></span></span>';
            print '</span>';
            print '<span class="topic-content">';
				print '<h2><a href="' . $vars['url'] . '/real-estate">Real Estate</a></h2> ';
				print '<span class="in-city-state">';
					print 'In ' . $vars['city'] . $vars['state'];
				print '</span>';
				print '<span class="presented-by">';
					print drupal_render($realestatead);
				print '</span>';
				print '<p>';
					print 'Ready to move to ' . $vars['city'] . '? There are a variety of housing options in the area. Click below to learn about the recent trends, average home values and even a few current real estate listings in ' . $vars['city'] . '.';
				print '</p>';
				print '<span class="topic-links">';
					print '<a href="' . $vars['url'] . '/real-estate/housing">Housing in ' . $vars['city'] . $vars['state'] . '</a><br/>';
					print '<a href="' . $vars['url'] . '/real-estate/home-values">Home Values in ' . $vars['city'] . $vars['state'] . '</a>';
				print '</span>';
			print '</span>';
		print '</li>';
	print '</ul>';
print '</div>';
